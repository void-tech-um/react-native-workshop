import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
  label: {
    fontSize: 16,
    textAlign: "center",
  },
  buttonWrapper: {
    width: "100%",
    alignItems: "center",
    justifyContent: "center",
    padding: 40,
  },
  addItemButton: {
    width: 200,
    height: 50,
    backgroundColor: "#4cff65",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 10,
  },
});

export default styles;
